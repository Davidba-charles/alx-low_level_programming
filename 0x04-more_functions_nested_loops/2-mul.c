#include "main.h"
#include <stdio.h>
/**
 * mul - multiply 2 integers
 * @a: this is to be multiplied by b
 * @b: this is to be multiplied by a
 * Return: return the result of a*b
 */
int mul(int a, int b)
{
return (a * b);
}
